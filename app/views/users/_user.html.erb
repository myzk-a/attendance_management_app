<li>
  <%= link_to user.name, user %> | <%= link_to "編集", edit_user_path(user) %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "削除", user, method: :delete, data: { confirm: "削除しますか?" } %>
  <% end %>
</li>